# Connect Da Dots — Obsidian Theme Submission

Use this when opening the community theme PR in `obsidianmd/obsidian-releases`.

## `community-css-themes.json` entry

```json
{
  "name": "Connect Da Dots",
  "author": "Kevork @ Nexacrawl",
  "repo": "Kevork-Nexacrawl-dev/obsidian-theme-Connect-Da-Dots",
  "screenshot": "Screenshot.png",
  "modes": ["dark"]
}
```

## Pre-submit checklist

- [x] `README.md` is present in the repo root
- [x] `LICENSE` is present in the repo root
- [x] `manifest.json` is present in the repo root
- [x] `theme.css` is present in the repo root
- [x] `Screenshot.png` is present in the repo root
- [x] Screenshot is `512x288` (16:9 recommended)
- [x] Repo slug matches the live GitHub repository
- [x] Theme is marked as dark-only

## PR title

```text
Add Connect Da Dots theme
```

## Notes

- If review feedback comes in, update this same repo and respond on the same PR thread.
- Keep the screenshot path and repo slug exactly aligned with the repo contents.
